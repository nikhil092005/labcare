document
  .getElementById("loginForm")
  .addEventListener("submit", function (event) {
    event.preventDefault();
    // Add actual login logic or AJAX call to backend authentication here
    alert("Login attempted. (Connect to backend for real login)");
  });
